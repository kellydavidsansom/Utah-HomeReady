<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Utah Home Ready Check</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .results-header {
            text-align: center;
            padding: 40px 20px;
            border-radius: 12px;
            margin-bottom: 30px;
        }
        .results-header.green { background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%); color: white; }
        .results-header.yellow { background: linear-gradient(135deg, #eab308 0%, #ca8a04 100%); color: white; }
        .results-header.red { background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%); color: white; }

        .results-header h1 {
            font-size: 2rem;
            margin-bottom: 10px;
        }
        .results-header .score {
            font-size: 3rem;
            font-weight: 700;
        }
        .results-header .score-label {
            font-size: 1rem;
            opacity: 0.9;
        }

        .results-container {
            max-width: 800px;
            margin: 0 auto;
        }

        .section {
            margin-bottom: 30px;
        }

        .section-title {
            font-size: 1.25rem;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid var(--border);
        }

        .affordability-cards {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
        }

        .affordability-card {
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            color: white;
        }

        .affordability-card.comfortable { background: var(--green); }
        .affordability-card.stretch { background: var(--yellow); }
        .affordability-card.strained { background: var(--red); }

        .affordability-card .label {
            font-size: 0.875rem;
            opacity: 0.9;
            margin-bottom: 5px;
        }

        .affordability-card .price {
            font-size: 1.5rem;
            font-weight: 700;
        }

        .affordability-card .payment {
            font-size: 0.875rem;
            margin-top: 5px;
            opacity: 0.9;
        }

        .affordability-card .mortgage {
            font-size: 0.75rem;
            margin-top: 3px;
            opacity: 0.85;
        }

        .section-title .down-payment {
            font-weight: normal;
            font-size: 1rem;
        }

        .ai-summary {
            background: var(--bg);
            padding: 25px;
            border-radius: 8px;
            line-height: 1.8;
        }

        .cta-section {
            text-align: center;
            padding: 40px;
            background: var(--white);
            border-radius: 12px;
            margin-top: 30px;
        }

        .cta-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 20px;
        }

        @media (max-width: 600px) {
            .affordability-cards {
                grid-template-columns: 1fr;
            }
            .cta-buttons {
                flex-direction: column;
            }
            .cta-buttons .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header', { agent }) %>

    <main class="main-content">
        <div class="container">
            <div class="alert alert-success" style="background: #22c55e; color: white; padding: 15px; border-radius: 8px; margin-bottom: 20px; text-align: center;">
                Your personalized report has been emailed to you!
            </div>
            <div class="results-container">
                <!-- Results Header -->
                <div class="results-header <%= lead.readiness_level %>">
                    <% if (lead.readiness_level === 'green') { %>
                        <h1>Congratulations, <%= lead.first_name %>!</h1>
                        <p>You're ready to buy a home!</p>
                    <% } else if (lead.readiness_level === 'yellow') { %>
                        <h1>Almost There, <%= lead.first_name %>!</h1>
                        <p>You're close to being ready</p>
                    <% } else { %>
                        <h1>Let's Build Your Path, <%= lead.first_name %>!</h1>
                        <p>We can help you get ready</p>
                    <% } %>

                    <div class="score"><%= lead.readiness_score %></div>
                    <div class="score-label">Readiness Score</div>
                </div>

                <!-- Affordability Section -->
                <div class="section">
                    <h2 class="section-title">Your Home Price Affordability Range <span class="down-payment">(with $<%= (lead.down_payment_saved || 0).toLocaleString() %> down)</span></h2>
                    <div class="affordability-cards">
                        <div class="affordability-card comfortable">
                            <div class="label">Comfortable</div>
                            <div class="price">$<%= (lead.comfortable_price || 0).toLocaleString() %></div>
                            <div class="mortgage">($<%= (lead.comfortable_loan || 0).toLocaleString() %> mortgage)</div>
                            <div class="payment">~$<%= (lead.comfortable_payment || 0).toLocaleString() %>/mo</div>
                        </div>
                        <div class="affordability-card stretch">
                            <div class="label">Stretch</div>
                            <div class="price">$<%= (lead.stretch_price || 0).toLocaleString() %></div>
                            <div class="mortgage">($<%= (lead.stretch_loan || 0).toLocaleString() %> mortgage)</div>
                            <div class="payment">~$<%= (lead.stretch_payment || 0).toLocaleString() %>/mo</div>
                        </div>
                        <div class="affordability-card strained">
                            <div class="label">Strained</div>
                            <div class="price">$<%= (lead.strained_price || 0).toLocaleString() %></div>
                            <div class="mortgage">($<%= (lead.strained_loan || 0).toLocaleString() %> mortgage)</div>
                            <div class="payment">~$<%= (lead.strained_payment || 0).toLocaleString() %>/mo</div>
                        </div>
                    </div>
                </div>

                <!-- AI Summary -->
                <% if (lead.ai_summary) { %>
                <div class="section">
                    <h2 class="section-title">Your Personalized Summary</h2>
                    <div class="ai-summary">
                        <%- lead.ai_summary %>
                    </div>
                </div>
                <% } %>

                <!-- Action Items -->
                <% if (actionItems && actionItems.length > 0) { %>
                <div class="section">
                    <h2 class="section-title">Your Next Steps</h2>
                    <% actionItems.forEach((item, index) => { %>
                        <div class="action-item">
                            <div class="action-item-icon"><%= index + 1 %></div>
                            <div class="action-item-content">
                                <h4><%= item.title %></h4>
                                <p><%= item.description %></p>
                            </div>
                        </div>
                    <% }); %>
                </div>
                <% } %>

                <!-- CTA Section -->
                <div class="cta-section">
                    <% if (lead.readiness_level === 'green' || lead.readiness_level === 'yellow') { %>
                        <h2>Ready to Take the Next Step?</h2>
                        <p>Get fully pre-approved and start your home search with confidence.</p>
                        <div class="cta-buttons">
                            <a href="/preapproval/<%= lead.id %>" class="btn btn-primary btn-large">Get Pre-Approved</a>
                        </div>
                    <% } else { %>
                        <h2>Let's Get You Home Ready!</h2>
                        <p>We have resources to help you improve your situation and get you on the path to homeownership.</p>
                        <div class="cta-buttons">
                            <% if (lead.red_light_reason === 'credit') { %>
                                <a href="/help/<%= lead.id %>/credit" class="btn btn-primary btn-large">Get Credit Help</a>
                            <% } else { %>
                                <a href="/help/<%= lead.id %>/boost" class="btn btn-primary btn-large">See Ways to Boost Your Buying Power</a>
                            <% } %>
                        </div>
                    <% } %>
                </div>

                <!-- Team Section -->
                <div class="section">
                    <h2 class="section-title">Your Team</h2>
                    <div class="team-section">
                        <% if (agent) { %>
                        <div class="team-member">
                            <% if (agent.headshot_url) { %>
                                <img src="<%= agent.headshot_url %>" alt="<%= agent.first_name %>">
                            <% } else { %>
                                <div style="width: 100px; height: 100px; border-radius: 50%; background: var(--border); margin: 0 auto 15px;"></div>
                            <% } %>
                            <h4><%= agent.first_name %> <%= agent.last_name %></h4>
                            <% if (agent.brokerage) { %>
                                <p><%= agent.brokerage %></p>
                            <% } %>
                            <p><%= agent.phone %></p>
                            <p><a href="mailto:<%= agent.email %>"><%= agent.email %></a></p>
                        </div>
                        <% } %>

                        <div class="team-member">
                            <div style="width: 100px; height: 100px; border-radius: 50%; background: var(--border); margin: 0 auto 15px;"></div>
                            <h4>Kelly Sansom</h4>
                            <p>ClearPath Utah Mortgage</p>
                            <p>NMLS #2510508</p>
                            <p>(801) 891-1846</p>
                            <p><a href="mailto:hello@clearpathutah.com">hello@clearpathutah.com</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
